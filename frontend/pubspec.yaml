name: construction_manager_app
description: "A production-grade Flutter application for construction site management."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2
  provider: ^6.1.2
  mobile_scanner: ^5.0.0
  encrypt: ^5.0.3
  intl: ^0.19.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
